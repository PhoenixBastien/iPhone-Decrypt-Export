version: "3.9"

services:
  iphone-decrypt-export:
    image: iphone-decrypt-export:latest
    build:
      context: .
    volumes:
      - "${BACKUP_ROOT}:/mnt/Backup:ro"
      - "${EXPORT_ROOT}:/mnt/Export"
    stdin_open: true
    tty: true
